import React from 'react'

const Empty = ({message}) => {
  return (
    <>
      <div className="rounded centered">{message}</div>
      <div className="rounded centered">{message}</div>
      <div className="rounded centered">{message}</div>
      <div className="rounded centered">{message}</div>
    </>
  )
}

export default Empty
